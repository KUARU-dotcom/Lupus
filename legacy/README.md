# legacy/

Архивные материалы предыдущих фаз разработки (V0.1–V0.2): реализация-прототип
на Python (`lupus_proto.py`), скрипты генерации датасетов и прогона экспериментов.

**Актуальные файлы находятся в корне репозитория**, не здесь:

- `run_experiment.py`, `run_experiment_Model_custom.py` → корень репозитория
- `lupus_cheatsheet.txt` → корень репозитория

Этот каталог сохранён как архив. Для работы с текущей версией Lupus (V0.3+)
используйте файлы в корне и `src/`.
