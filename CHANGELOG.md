# Changelog

## 0.3.0

Крупное расширение интерпретатора поверх V0.2.

### Область изменений (скоуп)

| Область | Статус |
|---------|--------|
| `cond` — многоусловный ветвитель | добавлено |
| `for … in (range …)` — циклы | добавлено |
| `let` — локальные связывания | добавлено |
| `match` — сопоставление с образцом | добавлено |
| `if-let` — условное связывание | добавлено |
| `assert` — утверждения | добавлено |
| Списки: `cons`/`head`/`tail`/`empty?`/`append` | добавлено/расширено |
| `list-map`/`list-filter`/`list-fold` | добавлено |
| Строки: `string-length`/`split`/`reverse` | добавлено |
| `str->int` (Option) | добавлено |
| Арифметика: `pow`/`min`/`max`/`abs`/`mod` | добавлено |
| `Option`: `some`/`none`; `Result`: `success`/`failure` | добавлено |
| `print` любого значения | добавлено |
| UTF-8 строковые литералы (кириллица, эмодзи) | исправлено |

### Исправлен баг UTF-8

Устранён баг разбора и печати строковых литералов в UTF-8, присутствовавший в V0.2
(раскрыт в Discussions репозитория). Строки с кириллицей и эмодзи теперь корректны:

```lisp
(print "привет 🐺")   ; → привет 🐺
```

### Бенчмарк

Бенчмарк `merged-lupus-2bv2`: **81/100**, без регрессии к V0.2.
